# 测试记录

## 1. 测试环境

- OS：Linux  
- 仓库路径：`/opt/kcsj/linux-agent-skills`  
- 执行：`bash tests/run_all_tests.sh`  

## 2. 用例表

| 编号 | 用例 | 预期 |
|------|------|------|
| TC-1 | `disk_check.sh` | 输出 df 表头与分区行，结尾 `OK` |
| TC-2 | `proc_health.sh` | 含 `uptime` 与 `ps` 表头 |
| TC-3 | `log_audit.sh` + 临时文件 | 能统计 ERROR/FAIL 计数 >0 |
| TC-4 | `agent-router.sh --dry-run` | 打印将执行脚本路径 |
| TC-5 | `safe_exec.py -- df -h` | 正常输出；`rm -rf /` 被拒绝 |

## 3. 实际结果摘录

（答辩前请粘贴 `tests/run_all_tests.sh` 终端输出截图说明至 `screenshots/`）

## 4. 已知限制

- 依赖系统是否安装 `vmstat`/`findmnt`。  
- 系统日志读取受文件权限约束。  
