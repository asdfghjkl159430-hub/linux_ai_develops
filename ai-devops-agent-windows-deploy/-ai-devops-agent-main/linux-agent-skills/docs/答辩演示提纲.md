# 答辩演示提纲（约 5 分钟）

## 0:00–0:30 项目目标

- Linux 运维 **Agent Skills** 课设：自然语言 → 受控 Shell 流水线。  
- 强调：**不是聊天接口**，而是 **工具调用 + 自动化编排**。

## 0:30–2:00 现场运行

1. `cd /opt/kcsj/linux-agent-skills`  
2. **Web**：`./scripts/start_web.sh`，浏览器打开 `http://127.0.0.1:8765/`，输入「检查磁盘空间」执行并展示 JSON 回显的 stdout。  
3. CLI：`./bin/agent-router.sh --dry-run "检查磁盘"` 与实跑对比。  
4. 展示 `logs/router-*.log` 与临时日志：`./runbooks/log_audit.sh /tmp/...`  

## 2:00–3:30 Linux 技术点

- 指出 `grep|awk|tail` 管道与 `df/ps` 命令。  
- 打开 `skills/log-analysis/SKILL.md` 说明 Agent 文档与脚本关系。

## 3:30–4:30 测试与工程

- `./tests/run_all_tests.sh` 快速通过。  
- `git log --oneline | head` 展示过程性提交。

## 4:30–5:00 总结

- 可扩展：增加 SSH 批量、Prometheus 文本导出等。  
- 风险：sudo 仅演示环境开启。
